const database = require("./assets/character_database.json");

const Web = require( "webwebweb" );
// Web.APIs[ "/" ] = ( qs, body, opts ) => {
//     return { "database": database };
//   };
Web.Run( 8080 );
